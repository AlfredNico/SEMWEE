export interface SettingTable {
    dispayColumns: any[],
    hiddenRows: any[],
    noHiddenRows: any[];
}

export interface SettingRowsTable {
    hiddenRows: string[],
    noHiddenRows: string[];
}
