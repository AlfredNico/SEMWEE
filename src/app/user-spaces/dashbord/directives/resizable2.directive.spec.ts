import { Resizable2Directive } from './resizable2.directive';

describe('Resizable2Directive', () => {
  it('should create an instance', () => {
    const directive = new Resizable2Directive();
    expect(directive).toBeTruthy();
  });
});
