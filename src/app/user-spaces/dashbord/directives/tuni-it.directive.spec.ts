import { TuniItDirective } from './tuni-it.directive';

describe('TuniItDirective', () => {
  it('should create an instance', () => {
    const directive = new TuniItDirective();
    expect(directive).toBeTruthy();
  });
});
