import { CapitalizeFirstPipePipe } from './capitalize-first-pipe.pipe';

describe('CapitalizeFirstPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizeFirstPipePipe();
    expect(pipe).toBeTruthy();
  });
});
